[project]
name = "hamster-cam"
version = "0.1.0"
description = "Hamster Cam - Gesture Recognition"
readme = "README.md"
requires-python = ">=3.10, <3.13"
dependencies = [
    "mediapipe==0.10.14",
    "numpy",
    "opencv-python",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.1",
]
